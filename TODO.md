- [ ] Update `styles.css`: Add CSS classes for staggered reveal animations (`.reveal`, `.active`).
- [ ] Update `styles.css`: Define background color variables for different sections if needed, or rely on JS to set them.
- [ ] Update `index.html`: Add `reveal` class to elements that should animate.
- [ ] Update `script.js`: Implement `IntersectionObserver` to trigger reveal animations on individual elements.
- [ ] Update `script.js`: Implement `IntersectionObserver` to change body background color based on the visible section.
